# 请求

部分接口支持批量请求，如：

```
GET /block/1
GET /block/2
GET /block/3
```

如果想用以下请求代替，请使用以下联系方式：

```
GET /block/1,2,3
```

在使用批量请求时，返回的结果为数组。

批量获取时，如果对应的值不存在，则为`null`。

如无特别说明，批量请求的最大数量为 50。

对于 POST 请求，均使用 JSON 编码。
